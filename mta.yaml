_schema-version: "3.2"
ID: hcrtvseriestvseries
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: hcrtvseriestvseries-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: hcrtvseriestvseries-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - hcrtvseriestvseries.zip
      name: hcrtvseriestvseries
      target-path: resources/
- name: hcrtvseriestvseries
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: hcrtvseriestvseries-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: hcrtvseriestvseries-html5-service
    service-plan: app-host
- name: hcrtvseriestvseries-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: hcrtvseriestvseries-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
